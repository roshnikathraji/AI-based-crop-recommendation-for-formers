
export interface FormData {
  soilType: string;
  temperature: string;
  rainfall: string;
  ph: string;
  region: string;
}

export interface CropRecommendation {
  cropName: string;
  reason: string;
}
